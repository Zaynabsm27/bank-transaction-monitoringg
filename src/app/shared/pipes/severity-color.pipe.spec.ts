import { SeverityColorPipe } from './severity-color.pipe';

describe('SeverityColorPipe', () => {
  it('create an instance', () => {
    const pipe = new SeverityColorPipe();
    expect(pipe).toBeTruthy();
  });
});
